# --- SEGURANÇA (OBRIGATÓRIO) ---
# Remova o # dessas duas linhas abaixo:
jwt.secret=${JWT_SECRET:secret_local_vibe_coding}
jwt.expiration=3600000

# --- CONFIGURAÇÃO H2 (ATIVO) ---
spring.datasource.url=jdbc:h2:mem:extratodb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Habilita o console do H2
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop

# Configura OpenAI
spring.ai.openai.api-key={OPENAI_API_KEY}
spring.ai.openai.chat.options.model=gpt-4o-mini
spring.ai.openai.chat.options.temperature=0.3


# --- CONFIGURAÇÃO POSTGRES (COMENTADO) ---
# spring.datasource.url=jdbc:postgresql://localhost:5432/extrato_popular
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.hibernate.ddl-auto=update



